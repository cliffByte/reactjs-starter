import { Meta, Controls, Canvas } from "@storybook/blocks";

import * as FileCard from "./FileCard.stories";

<Meta of={FileCard} />{" "}

# FileCard

FileCard component is used to display selected files. The component can take different file types by specifying the type. You can also view the file by clicking on the View Button.

<Canvas of={FileCard.Primary} />
## Inputs

FileRender has the following properties:

<Controls />
