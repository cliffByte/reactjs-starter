import image from "../../config/constant/Image";
const DashboardLogo = () => {
  return (
    <section className="dashboard_logo_section">
      <img src={image.logo} alt="logo" />
    </section>
  );
};

export default DashboardLogo;
